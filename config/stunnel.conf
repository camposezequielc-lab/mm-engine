; stunnel client config: plain TCP on 127.0.0.1:8080 -> TLS to reMarkets
; Windows: install stunnel from https://www.stunnel.org, run: stunnel.exe stunnel.conf
; Linux:   apt install stunnel4 && stunnel stunnel.conf
foreground = yes
debug = 5

[remarkets-fix]
client = yes
accept = 127.0.0.1:8080
connect = fix.remarkets.primary.com.ar:9876
