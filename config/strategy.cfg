# ---- Market making strategy parameters (assignment) ----
MINIMUM_SPREAD=0.05
ORDER_SPREAD=0.02
# DLR futures tick. VERIFY via SecurityList/reMarkets UI before going live.
TICK_SIZE=0.5
ORDER_QTY=1
# Book depth requested in MarketDataRequest (1..5 per ROFEX spec)
BOOK_DEPTH=5

# ---- Session ----
ACCOUNT=REM22479
FIX_CFG=config/fix_config_local.cfg
SYMBOLS_FILE=config/Actives.txt

# ---- Infrastructure ----
STRATEGY_CORE=2
FIX_CORE=3
METRICS_PORT=9091
LMDB_PATH=data/lmdb
SNAPSHOT_INTERVAL_MS=1000
# 1 = decide but never send orders (useful for first connection tests)
DRY_RUN=0
